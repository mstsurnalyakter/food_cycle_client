import React from 'react'
import { Link } from 'react-router-dom';

const NavLinkDetails = ({link}) => {
      <Link to={link}>
        <img src="/right-arrow2.png" alt="right-arrow" />
      </Link>;
}

export default NavLinkDetails